{
  "name": "web-console",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm -w run build:shared",
    "dev": "vite",
    "prebuild": "pnpm -w run build:shared",
    "build": "tsc -b && vite build",
    "pretypecheck": "pnpm -w run build:shared",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretypecheck:watch": "pnpm -w run build:shared",
    "typecheck:watch": "tsc -p tsconfig.json --noEmit -w --preserveWatchOutput",
    "preview": "vite preview",
    "dev:server": "pnpm -C ../gateway dev",
    "dev:all": "concurrently -n WEB,API -c auto \"pnpm dev\" \"pnpm -C ../gateway dev\"",
    "dev:mock-server": "tsx server/dev-sse-server.ts"
  },
  "dependencies": {
    "@eclia/symphony-protocol": "workspace:*",
    "@iconify/react": "^6.0.2",
    "@xyflow/react": "^12.10.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^6.26.2",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@eclia/config": "workspace:*",
    "@eclia/gateway-client": "workspace:*",
    "@eclia/utils": "workspace:*",
    "@types/node": "^20.19.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^5.1.2",
    "concurrently": "^9.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^7.0.0"
  }
}
