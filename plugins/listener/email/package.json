{
  "name": "plugin-listener-email",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -C ../../../packages/config build && tsx server/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:watch": "tsc -p tsconfig.json --noEmit -w --preserveWatchOutput"
  },
  "dependencies": {
    "@eclia/config": "workspace:*",
    "imapflow": "^1.0.196",
    "mailparser": "^3.9.3"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.6",
    "@types/node": "^20.19.31",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
