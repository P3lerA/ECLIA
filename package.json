{
  "name": "eclia",
  "private": true,
  "scripts": {
    "dev:web": "pnpm -C apps/web-console dev",
    "dev:api": "pnpm -C apps/gateway dev",
    "dev:all": "concurrently -n WEB,API \"pnpm -C apps/web-console dev\" \"pnpm -C apps/gateway dev\"",
    "build:web": "pnpm -C apps/web-console build",
    "dev:gateway": "pnpm -C apps/gateway dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
